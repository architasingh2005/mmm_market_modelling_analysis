import DashboardHome from './dashboard/DashboardHome';

// Dashboard — route entry point for /dashboard.
// All content is delegated to DashboardHome and its child components.
const Dashboard = () => <DashboardHome />;

export default Dashboard;
